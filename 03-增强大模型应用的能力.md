# 第 3 章 增强大模型应用的能力（试题占比 22%）

> **大纲要求**：模型不是什么都要靠记忆回答。掌握选型：查企业资料用 RAG，需要实时数据、确定性计算或业务动作时用工具。理解 RAG 的解析、切分、检索、重排序和评测会影响答案质量；了解 MCP 的作用，不要求掌握协议细节。
>
> 另需掌握：RAG、知识库服务和知识问答系统的基本原理与质量改进方法；工具调用、MCP 和个人 AI Agent 接入知识库与工具的方法。

## 3.1 为什么要增强大模型

大模型仅靠参数里的"记忆"回答问题存在三个短板（呼应第 1 章）：
1. 不掌握**私有知识**（企业文档、内部制度）；
2. 知识有**时间截止**，答不了实时信息；
3. 不能**执行动作**（查库、下单、算数）。

两条主要增强路线：**RAG（补知识）** 和 **工具调用（补动作与实时数据）**。

## 3.2 RAG 基本原理（高频考点）

**RAG（Retrieval-Augmented Generation，检索增强生成）**：回答问题前，先从知识库中检索出相关资料，把资料作为上下文交给大模型，让模型**基于资料生成答案**。

**核心价值**：让答案有据可查、减少幻觉，且无需重新训练模型即可更新知识。

### RAG 的标准流程（建库 → 检索 → 生成）

**建库阶段**：
1. **解析**：把 PDF、Word、网页等各种格式文档转换为纯文本（含表格、图片处理）；
2. **切分**：把长文本切成适当大小的片段（Chunk）。

**问答阶段**：
3. **检索**：根据用户问题，从所有片段中召回相关片段；
4. **重排序**：对召回结果按相关性精排，把最相关的片段排到前面；
5. **生成**：把排序后的资料连同问题一起交给大模型，生成有依据的答案。

### 各环节如何影响答案质量（高频考点）

| 环节 | 做不好的后果 | 改进方向 |
| --- | --- | --- |
| **解析** | 表格、扫描件识别错误 → 源头资料就是错的 | 针对文档类型优化解析方式，人工抽查 |
| **切分** | 切太碎丢上下文，切太大混入噪声；关键内容被从中间截断 | 调整片段大小与重叠，按标题/段落语义切分 |
| **检索** | 召回不到相关片段 → 模型无据可依，只能瞎编 | 优化关键词与语义检索，必要时人工标注同义词 |
| **重排序** | 相关资料排在后面甚至被截掉 | 引入重排序模型，调整召回数量 |
| **评测** | 不知道答案对不对，无法持续改进 | 建立问答评测集，量化准确率与引用质量 |

### 知识库质量改进的通用思路

- 源头治理：文档本身要准确、及时更新、去重去旧版；
- 结构治理：切分粒度合理、层级清晰；
- 效果治理：用真实问题建评测集，持续度量并迭代（呼应大纲"评测会影响答案质量"）。

## 3.3 知识库服务与知识问答系统

- **知识库服务**：托管文档的存储、解析、索引与检索能力，让企业文档变成可被 AI 引用的知识资产；
- **知识问答系统**：面向用户的问答入口 = 知识库 + RAG 流程 + 对话交互；
- 典型应用：企业内部知识问答、智能客服、产品文档助手。

## 3.4 工具调用（Function Calling）（高频考点）

- **工具调用**：大模型判断"需要什么能力"并给出调用参数，由外部程序**实际执行**（如查天气、算数据、调接口），再把执行结果返回给模型组织答案；
- 模型只负责**决策和解释**，不负责执行本身——因此结果的准确性由工具保证；
- **适用场景三要素**（高频考点）：
  1. **实时数据**：股价、天气、航班动态；
  2. **确定性计算**：精确数学运算、统计汇总；
  3. **业务动作**：查订单、发通知、写数据库。

## 3.5 RAG 与工具调用的选型（核心考点）

| 需求 | 选择 | 理由 |
| --- | --- | --- |
| 查企业资料、专业文档、制度规范 | **RAG / 知识库** | 知识在私有文档里，检索后引用作答 |
| 要实时数据 | **工具调用** | 知识库里没有"现在"的数据 |
| 要精确计算 | **工具调用** | 模型逐词生成不擅长精确计算 |
| 要执行业务动作 | **工具调用** | RAG 只能提供信息，不能执行动作 |
| 两者都需要 | **组合使用** | Agent 场景常见（第 5 章） |

## 3.6 MCP（高频考点，只考作用不考协议细节）

- **MCP（Model Context Protocol）**：一个开放标准，为大模型/Agent 接入外部工具和数据源提供**统一方式**；
- 类比：MCP 之于 AI 工具接入，类似 USB-C 之于电子设备——统一接口，即插即用；
- **核心价值**：工具/数据源只需按标准实现一次，就能被各种支持该标准的应用复用；避免每接一个模型就写一套适配；
- **大纲明确说明**：了解 MCP 的作用即可，**不要求掌握协议实现细节**。

## 3.7 个人 AI Agent 接入知识库与工具

- 个人 Agent（如 AI 办公助手）通过**知识库插件**接入个人/企业文档，通过**工具/MCP**接入外部能力；
- 使用要点：明确告知资料来源范围、控制权限边界、高风险操作人工确认（衔接第 5、6 章）。

## 3.8 常见误区

- ❌ "知识库建好就一劳永逸" → 文档更新、切分不当、评测缺失都会让质量退化，需要持续运营；
- ❌ "RAG 能保证答案 100% 正确" → RAG 减少幻觉但不消除：检索不准或模型不遵循资料时仍会出错；
- ❌ "工具调用就是让模型自己算" → 模型只负责决定调用哪个工具、传什么参数，执行由外部程序完成；
- ❌ "MCP 是一种新的大模型" → MCP 是工具接入标准，与模型本身无关。

## 自测题

**1.（单选）员工想查询公司最新的财务报销制度，最合适的技术方案是？**
A. 直接问大模型
B. 基于公司制度文档构建 RAG 知识问答
C. 对大模型做微调
D. 调用天气查询工具

<details>
<summary>答案与解析</summary>

**B**。私有文档知识用 RAG。微调不用于注入易变知识（第 4 章），A、D 明显不符。
</details>

**2.（单选）RAG 流程的正确顺序是？**
A. 检索 → 解析 → 切分 → 生成
B. 解析 → 切分 → 检索 → 重排序 → 生成
C. 生成 → 检索 → 重排序
D. 切分 → 解析 → 生成 → 检索

<details>
<summary>答案与解析</summary>

**B**。先建库（解析、切分），问答时检索、重排序，最后基于资料生成。
</details>

**3.（多选）以下哪些属于工具调用的典型适用场景？**
A. 获取实时股价
B. 计算 1024 × 768 的精确结果
C. 查询某员工的历史考勤记录并发起补卡流程
D. 把一首诗改写成散文风格

<details>
<summary>答案与解析</summary>

**ABC**。分别对应实时数据、确定性计算、业务动作三大场景。D 是纯文本生成任务，直接用大模型即可。
</details>

**4.（单选）关于 MCP 的说法，正确的是？**
A. MCP 是一种大模型训练协议
B. MCP 为大模型接入外部工具和数据源提供统一标准
C. MCP 只能用于阿里云的模型
D. 使用 MCP 后不再需要任何权限控制

<details>
<summary>答案与解析</summary>

**B**。MCP 是开放的工具/数据接入标准，与具体厂商无关，也不免除权限控制要求。
</details>

**5.（单选）RAG 问答系统经常"答非所问"，经排查发现相关文档片段根本没被检索到，最应优先改进的环节是？**
A. 重排序
B. 检索（召回）
C. 输出格式美化
D. 更换更大的模型

<details>
<summary>答案与解析</summary>

**B**。资料没被召回，问题出在检索环节；重排序只对已召回的结果精排。换模型解决不了"无据可依"的问题。
</details>

**6.（单选）知识库中的 PDF 表格经常被解析成乱码，导致答案错误，根源环节是？**
A. 切分
B. 检索
C. 解析
D. 重排序

<details>
<summary>答案与解析</summary>

**C**。格式转换出错属于解析环节的问题，源头资料错了，后面环节再好也救不回来。
</details>
