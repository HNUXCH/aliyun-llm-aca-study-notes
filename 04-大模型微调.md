# 第 4 章 大模型微调（试题占比 12%）

> **大纲要求**：微调不是万能按钮。掌握微调适合什么：稳定行为、格式、风格、分类能力，或支持轻量模型部署。理解微调的基本流程：定义目标、准备样本、选择基模、控制数据质量、评估效果。能区分：查知识用 RAG，做动作靠工具，稳行为才考虑微调。
>
> 另需掌握：微调的适用场景、基本流程，以及与提示词、RAG 的选型边界。

## 4.1 什么是微调

- **微调（Fine-tuning）**：在一个已有基础模型（基模）之上，用一批高质量样本继续训练，使模型更贴合特定任务的行为模式；
- 通俗理解：不是"从零造模型"，而是"在通用能力上做针对性强化"；
- **关键心态**：微调**不是万能按钮**——成本高、周期长，很多问题用提示词或 RAG 就能解决，先穷尽轻量手段再考虑微调。

## 4.2 微调适合什么（高频考点）

| 适合微调 | 说明 |
| --- | --- |
| **稳定行为** | 让模型在同类任务上表现一致、可预期 |
| **格式与风格** | 固定输出结构、语言风格、语气规范 |
| **分类能力** | 强化特定业务场景的分类判断 |
| **轻量模型部署** | 用小模型微调替代大模型调用，降低成本或满足私有化部署 |

## 4.3 微调不适合什么（高频考点）

| 不适合 | 原因 | 正确做法 |
| --- | --- | --- |
| 注入易变的事实知识 | 知识会过时，重新微调成本高 | 用 RAG |
| 查询实时信息 | 微调后知识仍是静态的 | 用工具调用 |
| 执行动作 | 微调改变不了"执行能力" | 用工具调用 |
| 偶尔的格式/语气问题 | 提示词里加约束更便宜 | 优化提示词 |

## 4.4 微调的基本流程（高频考点）

1. **定义目标**：明确要改善的具体行为，以及验收标准（先想清楚"怎样算成功"）；
2. **准备样本**：收集/标注训练样本，样本要覆盖目标场景；
3. **选择基模**：根据任务难度、成本与部署约束选合适的基础模型；
4. **控制数据质量**：样本要准确、一致、无冲突——**数据质量决定微调上限**；
5. **评估效果**：用独立评测集验证，对比微调前后的表现，防止过拟合或能力退化。

## 4.5 数据质量要点

- **一致性**：同类问题的示范答案风格统一，互相矛盾的样本会让模型困惑；
- **代表性**：覆盖真实业务中的主要情况和边界情况；
- **准确性**：错误示范会被模型学走——垃圾进，垃圾出；
- **数量适度**：不是越多越好，质量优先；过少学不到模式，过多增加成本。

## 4.6 三种手段的选型边界（核心考点，全卷主线）

| 需求 | 首选手段 |
| --- | --- |
| 改变/明确单次任务的要求 | **提示词**（最便宜、即时生效） |
| 让回答依据私有/最新资料 | **RAG / 知识库** |
| 实时数据、精确计算、业务动作 | **工具调用** |
| 长期稳定的行为、格式、风格、分类；轻量部署 | **微调** |

**决策顺序口诀**：提示词 → RAG/工具 → 微调。前面的手段能解决就不上后面的。

**记忆金句**（大纲原文精神）：**查知识用 RAG，做动作靠工具，稳行为才考虑微调。**

## 4.7 常见误区

- ❌ "模型答错了，微调一下就好" → 先判断错因：缺知识→RAG，缺实时数据/动作→工具，行为不稳定才轮到微调；
- ❌ "微调能教会模型新知识" → 微调擅长塑造行为模式，不是可靠的知识注入手段；
- ❌ "样本越多微调效果越好" → 质量优先，错误样本会教坏模型；
- ❌ "微调完就不用评测了" → 必须用独立评测集验证，防止能力退化。

## 自测题

**1.（单选）企业希望客服机器人始终以固定格式输出：先致歉、再给方案、最后给跟进入口。最合适的手段是？**
A. 提示词中说明格式要求，若需长期稳定一致再考虑微调
B. 用 RAG 接入格式规范文档
C. 调用格式化工具
D. 直接重新训练一个新模型

<details>
<summary>答案与解析</summary>

**A**。格式属于行为/格式问题，先用提示词；要求长期稳定一致时微调是合适手段。B 用于知识，C 用于动作，D 成本过高。
</details>

**2.（单选）"模型不了解公司上个月新发布的产品"，正确的解决方式是？**
A. 微调
B. RAG 接入产品资料
C. 增大上下文窗口
D. 深度思考模式

<details>
<summary>答案与解析</summary>

**B**。新知识、易变知识用 RAG。微调不适合注入易变事实，成本高且知识会过时。
</details>

**3.（多选）以下哪些是微调的适用场景？**
A. 稳定特定任务的输出风格
B. 强化业务分类能力
C. 用小模型微调后私有化部署
D. 查询实时天气

<details>
<summary>答案与解析</summary>

**ABC**。D 是实时数据场景，应使用工具调用。
</details>

**4.（单选）微调流程中，决定效果上限的关键因素是？**
A. 基模的参数规模
B. 样本数据的质量
C. 训练时长
D. 评测集的大小

<details>
<summary>答案与解析</summary>

**B**。数据质量决定微调上限：错误、矛盾的样本会让模型学坏。其他因素有影响但非决定性。
</details>

**5.（单选）以下手段按成本从低到高的合理尝试顺序是？**
A. 微调 → RAG → 提示词
B. 提示词 → RAG/工具 → 微调
C. RAG → 微调 → 提示词
D. 工具 → 微调 → RAG

<details>
<summary>答案与解析</summary>

**B**。先用最便宜的提示词，再上 RAG/工具，最后才考虑微调——这是大纲强调的选型边界。
</details>
