# 第 4 章 大模型微调（试题占比 12%）

> **大纲要求**：微调不是万能按钮。掌握微调适合什么：稳定行为、格式、风格、分类能力，或支持轻量模型部署。理解微调的基本流程：定义目标、准备样本、选择基模、控制数据质量、评估效果。能区分：查知识用 RAG，做动作靠工具，稳行为才考虑微调。
>
> 另需掌握：微调的适用场景、基本流程，以及与提示词、RAG 的选型边界。
>
> **官方课程核心观点**（课时5）：微调类似考生应对闭卷考试——准备数据集复杂、反复尝试参数组合成本高，且可能导致"灾难性遗忘"。微调不是首选，而是特定场景下的定制手段。

## 4.1 什么是微调

- **微调（Fine-tuning）**：在预训练基础上，使用特定领域数据对模型进一步训练，让模型更懂你的专业领域、更符合特定需求；
- 通俗理解：给已经训练好的聪明学生进行针对性补习；
- **"微"字体现在**：数据规模远小于预训练；训练时间较短（基模已学过通用知识）；
- ⚠️ **灾难性遗忘**：微调后模型可能除了这件事别的什么也不会干。高效微调方法虽不改变原始参数，但具体实例仍会变为专事专用模块。

## 4.2 微调能实现什么（高频考点）

| 场景 | 说明 |
| --- | --- |
| **风格化（角色扮演）** | 让模型作为医疗专家、法律顾问等特定角色回答，切中要害给出专业建议 |
| **格式化（系统对接）** | 让模型输出满足系统 API 格式要求的指令，实现用户提问到调用服务的端到端自动化 |
| **降本提效** | 用小模型微调替代大模型调用，在可接受准确度损失下大幅降低推理成本和耗时 |

## 4.3 微调的关键挑战

- **高质量数据不易获得**：收集过程带来成本和时间挑战；
- **参数调优需反复迭代**：训练轮次过小、学习率过大等都可能导致过拟合或欠拟合，消耗大量时间和资金；
- **过拟合**：训练数据上表现好，测试/实际应用中表现差（只记住答案不理解本质）；
- **欠拟合**：训练过程过于简单，训练和测试数据上表现都不好。

## 4.4 业务决策四要素（高频考点）

决定是否微调前，评估：

1. **业务需求匹配度**：任务是否需要复杂语言理解/生成？是否高度领域特定？当前模型是否已能满足？有无具体业务指标衡量前后对比？
2. **数据可用性与质量**：能否提取足够标注数据？质量、代表性是否满足要求？
3. **合规与隐私**：数据符合法规（如 GDPR），关注模型偏见、过拟合、泛化不足等风险；
4. **资源与技术可行性**：GPU、存储、专家人力成本；团队技术能力或合作伙伴支持；
5. **ROI 分析**：微调带来的商业价值（直接经济效益+用户体验提升等间接效益）是否超过成本。

## 4.5 微调的基本流程（高频考点，官方九步）

1. **数据准备**：收集交互记录/常见问答 → 清洗去敏感信息；
2. **模型选择**：选适合的预训练基模（如 Qwen-7B-chat）；
3. **模型微调**：标注数据（如需）→ 运行微调程序调整参数；
4. **模型评测**：准备与训练集格式一致的评测集，通过百炼评测功能查看表现；
5. **模型集成**：编写代码集成到业务流程，处理输出/API错误/权限；
6. **测试与优化**：在开发测试环境验证，根据结果优化；
7. **模型部署**：生产环境部署；
8. **监控与维护**：监控性能和 API 健康，定期重评优化；
9. **持续迭代**：根据用户反馈和业务变化改进。

> 大纲简化版五步：定义目标、准备样本、选择基模、控制数据质量、评估效果。官方九步是完整工程流程，考试以大纲五步为主，了解九步有助于理解全貌。

## 4.6 微调方式：全参微调 vs 高效微调 PEFT（高频考点）

### 全参微调（Full Fine Tuning）

- 在预训练基础上全量参数微调；
- 避免从头训练的资源消耗，也避免部分参数未微调导致性能下降；
- 但训练成本仍高昂。

### 高效微调 PEFT（Parameter-Efficient Fine-Tuning）

旨在降低微调参数数量和计算复杂度，同时保持接近全参微调的效果。主流方法：

| 方法 | 原理 | 特点 |
| --- | --- | --- |
| **LoRA** | 不改原模型，旁路增加低秩矩阵 A×B 近似权重更新，只训练低秩矩阵 | 最通用、效果接近全参、可扩展性好 |
| **Adapter Tuning** | 在模型层间插入 Adapter 层，只训练 Adapter | 增加约 3.6% 参数达全参效果；引入推理延迟 |
| **Prefix Tuning** | 每层 Transformer 保留可训练前缀向量 | 避免灾难性遗忘，长序列生成优异；占用上下文窗口，难优化 |
| **Prompt Tuning** | 仅在输入层加可训练 prompt tokens | 最轻量，有时加 1 个 token 也有效；更适合分类问答，复杂生成不如 Prefix |

> **关键结论**：并不是训练参数越多效果越好。LoRA 是较为高效的微调方法。

## 4.7 三种手段的选型边界（核心考点，全卷主线）

| 需求 | 首选手段 |
| --- | --- |
| 改变/明确单次任务的要求 | **提示词**（最便宜、即时生效） |
| 让回答依据私有/最新资料 | **RAG / 知识库** |
| 实时数据、精确计算、业务动作 | **工具调用** |
| 长期稳定的行为、格式、风格、分类；轻量部署 | **微调** |

**决策顺序口诀**：提示词 → RAG/工具 → 微调。前面的手段能解决就不上后面的。

**记忆金句**（大纲原文精神）：**查知识用 RAG，做动作靠工具，稳行为才考虑微调。**

## 4.8 阿里云微调产品选择

| 用户背景 | 推荐产品 |
| --- | --- |
| 有算法背景，希望掌控细节 | PAI（DSW/DLC/EAS） |
| 已有 VM/容器训练经验 | GPU 云服务器 / ACK 云原生 AI 套件 |
| 无算法背景 | 百炼（开箱即用界面化调优+评测） |

> ⚠️ 微调训练成本与模型规模相关，小模型（如 4B）也会产生数元训练费用，提前准备预算。

## 4.9 常见误区

- ❌ "模型答错了，微调一下就好" → 先判断错因：缺知识→RAG，缺实时数据/动作→工具，行为不稳定才轮到微调；
- ❌ "微调能教会模型新知识" → 微调擅长塑造行为模式，不是可靠的知识注入手段；且可能导致灾难性遗忘；
- ❌ "样本越多微调效果越好" → 质量优先，错误样本会教坏模型；
- ❌ "微调完就不用评测了" → 必须用独立评测集验证，防止能力退化；
- ❌ "训练参数越多效果越好" → LoRA 等方法证明少量参数也能接近全参效果；
- ❌ "微调是首选方案" → 微调成本高、周期长、可能灾难性遗忘，应作为最后手段。

## 自测题

**1.（单选）企业希望客服机器人始终以固定格式输出：先致歉、再给方案、最后给跟进入口。最合适的手段是？**
A. 提示词中说明格式要求，若需长期稳定一致再考虑微调
B. 用 RAG 接入格式规范文档
C. 调用格式化工具
D. 直接重新训练一个新模型

<details>
<summary>答案与解析</summary>

**A**。格式属于行为/格式问题，先用提示词；要求长期稳定一致时微调是合适手段。B 用于知识，C 用于动作，D 成本过高。
</details>

**2.（单选）"模型不了解公司上个月新发布的产品"，正确的解决方式是？**
A. 微调
B. RAG 接入产品资料
C. 增大上下文窗口
D. 深度思考模式

<details>
<summary>答案与解析</summary>

**B**。新知识、易变知识用 RAG。微调不适合注入易变事实，成本高且知识会过时。
</details>

**3.（多选）以下哪些是微调的适用场景？**
A. 稳定特定任务的输出风格
B. 强化业务分类能力
C. 用小模型微调后私有化部署
D. 查询实时天气

<details>
<summary>答案与解析</summary>

**ABC**。D 是实时数据场景，应使用工具调用。
</details>

**4.（单选）关于 LoRA 的说法，正确的是？**
A. LoRA 需要调整模型全部参数
B. LoRA 通过旁路低秩矩阵近似权重更新，只训练少量参数，效果接近全参微调
C. LoRA 会显著增加推理延迟
D. LoRA 只适用于图像生成任务

<details>
<summary>答案与解析</summary>

**B**。LoRA 是最高效通用的 PEFT 方法之一，不改原模型，通过低秩矩阵压缩训练参数。A、C、D 均错误。
</details>

**5.（单选）以下手段按成本从低到高的合理尝试顺序是？**
A. 微调 → RAG → 提示词
B. 提示词 → RAG/工具 → 微调
C. RAG → 微调 → 提示词
D. 工具 → 微调 → RAG

<details>
<summary>答案与解析</summary>

**B**。先用最便宜的提示词，再上 RAG/工具，最后才考虑微调——这是大纲强调的选型边界。
</details>

**6.（单选）微调后模型除了特定任务外其他能力明显下降，这种现象称为？**
A. 过拟合
B. 欠拟合
C. 灾难性遗忘
D. 幻觉

<details>
<summary>答案与解析</summary>

**C**。灾难性遗忘指微调后模型丧失原有通用能力。过拟合是训练好测试差，欠拟合是两者都差，幻觉是编造内容。
</details>
