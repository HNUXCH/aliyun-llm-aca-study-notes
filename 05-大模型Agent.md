# 第 5 章 大模型 Agent（试题占比 16%）

> **大纲要求**：Agent 的价值是把"回答问题"推进到"完成任务"。掌握如何给 Agent 明确目标、资料来源、工具边界和阶段检查点。理解 Skill 用来固化重复工作方法，自检清单要具体、可核对、有证据。高风险动作必须人工确认。
>
> 另需掌握：AI Agent 的基本能力、适用场景、局限性和协作使用方式；Skill、自检闭环等帮助 Agent 沉淀方法和提升输出质量的实践方式。

## 5.1 什么是 AI Agent

- **AI Agent（智能体）**：以大模型为"大脑"，能够**自主规划步骤、调用工具、多轮迭代直至完成任务**的系统；
- **核心区别**（高频考点）：
  - 普通大模型对话 = **回答问题**（一问一答）；
  - Agent = **完成任务**（理解目标 → 拆解计划 → 调用工具执行 → 检查结果 → 迭代修正）。

## 5.2 Agent 的基本能力

1. **目标理解与任务规划**：把复杂目标拆成可执行的步骤；
2. **工具调用**：按计划调用外部工具完成具体动作（查数据、读写文件、发消息等）；
3. **记忆与上下文管理**：在执行过程中记住中间结果和状态；
4. **反思与迭代**：检查结果不满意时调整方法重试。

## 5.3 适用场景与局限性（高频考点）

**适合交给 Agent 的**：
- 步骤可拆解、过程可观察的任务（调研整理、资料分析、办公自动化）；
- 重复性、流程化的工作；
- 需要组合多个工具/知识源的复合任务。

**不适合 / 需谨慎的**：
- 结果不可验证的任务（无从判断做没做对）；
- 高风险且不可逆的动作（删除数据、对外发布、资金操作）→ 必须人工确认；
- 需要人类价值判断与责任承担的最终决策。

## 5.4 如何给 Agent 下达任务（高频考点，四要素）

| 要素 | 说明 |
| --- | --- |
| **明确目标** | 说清楚最终要得到什么、验收标准是什么 |
| **资料来源** | 指明依据哪些文档/数据，范围边界在哪 |
| **工具边界** | 允许用哪些工具、哪些操作不能做 |
| **阶段检查点** | 在关键节点停下来让人确认，再继续往下走 |

> 与第 2 章呼应：给 Agent 下达任务 = 讲清楚目标、背景、约束、验收标准；Agent 只是把这些要求落实到多步执行上。

## 5.5 Skill：固化重复工作方法（高频考点）

- **Skill（技能）**：把一套经过验证的工作方法（步骤、规范、工具用法）沉淀成可复用的模块，供 Agent 在遇到同类任务时直接调用；
- **价值**：
  1. 把"个人经验"变成"组织能力"，同类任务不用每次重新摸索；
  2. 保证输出质量稳定一致；
  3. 新方法经实践验证后可持续积累迭代。
- 类比：Skill 之于 Agent，类似标准作业手册之于员工。

## 5.6 自检闭环（高频考点）

- **自检闭环**：Agent 完成产出后，用一份**自检清单**逐项核对，发现问题就修正，直到通过为止；
- 自检清单的三要素（高频考点）：**具体、可核对、有证据**：
  - **具体**：条目明确到可操作（"核对每个数据点是否标注来源"，而非"内容要准确"）；
  - **可核对**：每一项都能判断"通过/不通过"；
  - **有证据**：结论要能出示依据（引用原文、运行结果、截图等）。
- 自检闭环是提升 Agent 输出质量的关键实践，也是"人工复核"前的第一道质量防线。

## 5.7 协作使用方式与人工确认

- **人机协作模式**：人负责目标、判断与责任，Agent 负责执行与初稿；人在检查点上把关；
- **高风险动作必须人工确认**（与安全合规章联动）：
  - 对外发送/发布内容；
  - 删除、覆盖数据或文件；
  - 涉及资金、权限、合同的变更；
  - 影响他人或共享环境的操作。
- 设计原则：**让 AI 在边界内工作**——明确授权范围、保留关键节点的人工决策权。

## 5.8 常见误区

- ❌ "Agent 可以全自动完成一切，不需要人管" → Agent 有局限性，高风险动作必须人工确认，关键节点设检查点；
- ❌ "Skill 就是一种插件商店" → Skill 的本质是**沉淀经过验证的工作方法**，重点在方法复用与质量稳定；
- ❌ "自检清单写'保证质量'就行" → 清单必须具体、可核对、有证据，笼统条目无法执行；
- ❌ "任务交代得越简单，Agent 发挥越好" → 缺少目标、资料、边界、检查点的任务，Agent 容易跑偏。

## 自测题

**1.（单选）AI Agent 与普通大模型对话的本质区别是？**
A. Agent 回答速度更快
B. Agent 能把"回答问题"推进到"完成任务"
C. Agent 不需要大模型
D. Agent 不会产生幻觉

<details>
<summary>答案与解析</summary>

**B**。大纲原文：Agent 的价值是把"回答问题"推进到"完成任务"。Agent 依然基于大模型，也会有幻觉。
</details>

**2.（多选）给 Agent 下达任务时，应当明确的内容包括？**
A. 任务目标与验收标准
B. 资料来源与范围
C. 工具使用边界
D. 阶段检查点

<details>
<summary>答案与解析</summary>

**ABCD**。四要素齐备：明确目标、资料来源、工具边界、阶段检查点。
</details>

**3.（单选）关于 Skill 的作用，下列说法正确的是？**
A. Skill 用于给模型注入实时数据
B. Skill 用来固化重复工作方法，让同类任务质量稳定可复用
C. Skill 是替代人工确认的机制
D. Skill 只在训练阶段有用

<details>
<summary>答案与解析</summary>

**B**。Skill 把经过验证的工作方法沉淀为可复用模块；实时数据靠工具，人工确认不可替代。
</details>

**4.（单选）以下哪一项符合自检清单的要求？**
A. "内容尽量写好"
B. "保证数据准确无误"
C. "核对每个引用的数据点是否标注了来源，未标注的补上"
D. "输出要专业"

<details>
<summary>答案与解析</summary>

**C**。自检清单必须具体、可核对、有证据。A、B、D 都是笼统表述，无法逐项核对。
</details>

**5.（单选）Agent 准备执行"删除过期客户数据"的操作，正确的做法是？**
A. 直接执行以提高效率
B. 先人工确认，再执行
C. 让 Agent 自行判断是否删除
D. 跳过确认，事后抽查

<details>
<summary>答案与解析</summary>

**B**。删除数据属于高风险、不可逆动作，必须人工确认后再执行。
</details>
