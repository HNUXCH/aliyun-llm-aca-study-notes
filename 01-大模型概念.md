# 第 1 章 大模型概念（试题占比 18%）

> **大纲要求**：理解大模型是什么，能做什么，不能直接替人决定什么。理解大模型基于上下文生成内容的基本方式，以及由此带来的幻觉、训练数据时间范围和上下文限制。掌握能力边界判断：什么时候适合生成、总结、分析，什么时候需要知识库、工具或人工确认。

## 1.1 什么是大模型

- **AI（人工智能）**：只要机器能表现出智能行为（识别图片、听懂语音、翻译、下棋、推荐等），都属于 AI。
- **大模型**：AI 里很重要的一类模型。通过大量数据训练，学到语言、图像、代码和知识中的规律。特点是**通用**：同一个模型可以写作文、做总结、翻译、看图、写代码。
- **Agent**：不是另一个大模型，而是一种**使用大模型的方式**。大模型负责理解和生成；Agent 在它旁边接上工具、记忆和执行流程，让 AI 不只是回答问题，还能一步步完成任务。
- 类比理解：
  - 大模型是"大脑"，负责思考和表达；
  - 工具是"手和工具箱"，负责查资料、读文件、算数据、画图；
  - Agent 是"数字助理"，把大脑、工具和流程组织起来，把任务做完。

### 大模型与传统方法的区别

| 阶段 | 方法 | 局限 |
| --- | --- | --- |
| 规则系统 | 手写规则做判断 | 真实世界太复杂，规则写不完，新情况不断出现 |
| 传统机器学习 | 从样例中找规律 | 一个任务配一个模型，换任务需重新准备 |
| **大模型** | 同一模型处理多种任务 | 通用助手，调用已学到的语言、知识和模式 |

### 大模型的"大"体现在三个方面

- **参数规模大**：参数是模型内部保存规律的"旋钮"，参数越多，可表达的模式越复杂；
- **训练数据多**：需要从大量文本、代码、图片、音频等数据中学习；
- **适用任务广**：同一模型可处理问答、总结、翻译、写作、代码、多模态理解等。

> ⚠️ "大"不自动等于"永远正确"。模型见过很多内容，不代表知道最新信息；能生成流畅回答，不代表每句话都有可靠依据。

### 模型、平台和应用的关系

| 层次 | 职责 | 举例 |
| --- | --- | --- |
| 模型 | 负责理解和生成 | 千问、万相 |
| 平台 | 把模型用起来（体验、调用、接入知识库和工具） | 百炼 |
| 应用 / Agent 工具 | 面向具体任务 | Qoder、千问办公、听悟 |

## 1.2 大模型怎样生成回答（高频考点）

### 文字接龙：自回归生成

- 大模型生成文字时，根据前面的内容**预测下一个 token**，再把它接到已有内容后面，继续预测下一个 token。一个回答是一步步接出来的。
- 每一步出现的不是唯一答案，而是**一组候选**及其概率。
- 这解释了：同样一句话问两次，回答可能不完全一样——生成过程本来就有概率。
- 这个过程叫**自回归生成（Auto-regressive Generation）**。

### Token：模型眼里的文字片段

- 大模型生成的不一定是"一个字"或"一个词"，而是 **token**（文字片段）。
- 例："今天天气"可能被切成"今天 / 天气"两个 token；"strawberry"可能被切成"str / aw / berry"。
- 模型处理的是切分后的片段，再转换成数字 ID。
- 后续讲上下文长度、计费、长文处理时都会用到 token。

### 注意力机制

- 生成每个 token 时，模型会**回看前面的内容**，判断哪些位置更重要。
- 模型能顺着上下文生成句子，是因为它在前面 token 序列之间找到了相关线索。

### MoE（混合专家模型）

- 模型越来越大，成本、速度和资源消耗成为问题。MoE 是解决效率问题的思路。
- 模型内部有多组"专家"，每次生成时只激活其中一部分，而非所有参数一起参与计算。
- 例：Qwen3-235B-A22B，总参数 235B（约 2350 亿），每次推理只激活 22B（约 220 亿）。
- MoE 的价值：知识容量大，单次计算更省，推理速度更容易优化。

## 1.3 大模型怎样学会当助手：训练三阶段

| 阶段 | 目标 | 做法 |
| --- | --- | --- |
| **预训练** | 获得语言和知识 | 大量阅读（新闻、书籍、网页、代码等），反复练习"根据前面的 token 预测下一个 token" |
| **SFT（监督微调）** | 从"会续写"变成"会按要求回答" | 给模型看大量高质量"指令-回答"样本，学会按指令格式输出 |
| **偏好对齐** | 让回答更符合人的期待 | RLHF（人类反馈强化学习）或 DPO（直接偏好优化），让模型更会表达、该拒绝时拒绝 |

**预训练数据质量的影响**：

| 数据情况 | 可能影响 |
| --- | --- |
| 数据少 | 知识有限，容易犯低级错误 |
| 数据多但质量低 | 可能学到错误、重复、偏见或无用信息 |
| 数据多且质量高 | 泛化能力和理解力通常更强 |

> 预训练数据通常要经过清洗和治理：去重、去噪、过滤低质量内容，移除有害内容，处理个人信息，覆盖不同语言和领域。

## 1.4 三大常见边界（高频考点）

| 边界 | 含义 | 典型表现 | 应对方法 |
| --- | --- | --- | --- |
| **上下文有限** | 一次能"看见"的内容有上限 | 长资料漏掉中间细节；记住前半段忘了后半段约束 | 先拆分、先摘要、先明确要查什么 |
| **知识可能过时** | 知识来自训练数据，有时间范围 | 不知道训练后发生的事；可能给出基于旧信息的答案 | 给最新材料，或通过工具查实时信息 |
| **回答可能不准确（幻觉）** | 通顺不等于真实；可能编造出处、补无依据的数字 | 错的时候也可能很自信 | 重要结论要求给出依据，自己抽查关键点 |

**幻觉的根源**：生成机制决定模型是在"预测最可能的下文"，不是在"查询事实库"；加上训练数据本身有噪声和错误。因此**幻觉只能减少、无法彻底消除**。

**使用原则**：短任务直接问，长资料先拆；旧知识让它查新资料；重要结论要核查来源。

## 1.5 能力边界判断（核心考点）

考试重点不是背定义，而是**给一个场景，判断该用哪种方式**：

| 场景特征 | 正确做法 |
| --- | --- |
| 创意写作、润色改写、总结、翻译、头脑风暴 | 直接用大模型生成 |
| 规则明确的分类、格式转换、信息提取 | 直接用大模型（配合好的提示词） |
| 答案必须依据企业私有资料 / 专业文档 | 用 **RAG / 知识库**（第 3 章） |
| 需要实时数据、精确计算、执行业务动作 | 用**工具调用**（第 3 章） |
| 需要稳定的格式 / 风格 / 行为模式 | 考虑**微调**（第 4 章） |
| 医疗诊断、金融决策、合同签署、对外发布 | AI 辅助 + **人工确认**（第 6 章） |

**一句话口诀**：生成靠模型，事实靠 RAG，动作靠工具，稳定靠微调，责任靠人工。

## 1.6 典型应用场景

### 三个层次

| 层次 | 说明 | 举例 |
| --- | --- | --- |
| 内容生成与分析 | 用提示词生成文案、摘要、分析数据 | 广告文案、会议纪要提取、销售趋势分析 |
| 智能体端到端完成工作 | 多步骤任务：读文件→分析→生成报告 | 经营分析报告、PPT 生成、制作小工具 |
| 发现机会与辅助决策 | 在信息不充分时帮人找方向、筛线索 | Deep Research、科学发现、新材料筛选 |

### 阿里云千问系列产品

| 产品 | 定位 |
| --- | --- |
| 千问 | 超大规模语言模型，复杂指令理解、创作、数学、代码、多语言多模态 |
| 万相 | 多模态图像和视频生成模型（文生图、图生图、图生视频等） |
| 百炼 | 大模型服务平台，提供模型调用、训练开发、应用构建 |
| Qoder | 面向开发场景的 AI 工具（读代码、写代码、解释代码） |
| 千问办公 | 桌面 AI 助理，读取文件、整理资料、调用工具，完成任务 |
| 听悟 | 智能语音转文字和内容分析（会议记录、字幕、笔记） |
| 法睿 | 法律服务（合同审核、法律咨询、文书起草） |

## 1.7 常见误区

- ❌ "大模型就是更聪明的搜索引擎" → 搜索引擎是检索已有网页，大模型是**生成**内容，两者机制不同；
- ❌ "模型回答得越流畅，说明它越'知道'事实" → 流畅来自语言模式学习，与事实正确性无必然关系；
- ❌ "换了更大的模型就不会有幻觉" → 幻觉是生成机制的固有属性，只能缓解不能根除；
- ❌ "AI 可以替人做最终决定" → 高风险场景的最终责任必须在人；
- ❌ "Agent 是另一种大模型" → Agent 是使用大模型的方式，核心仍是大模型，只是接入了工具、记忆和流程；
- ❌ "预训练数据越多越好" → 数据质量同样重要，低质量数据会带来错误、偏见和噪声。

## 自测题

**1.（单选）用户询问"公司最新发布的差旅报销标准"，最合适的处理方式是？**
A. 直接让大模型回答
B. 让大模型联网搜索公开信息后回答
C. 通过 RAG 接入公司制度文档后回答
D. 对大模型进行微调后再回答

<details>
<summary>答案与解析</summary>

**C**。企业私有制度文档不在模型训练数据中，公开搜索也查不到内部制度；正确做法是用 RAG 接入公司文档。微调不适合用来"注入知识"（见第 4 章）。
</details>

**2.（单选）大模型产生"幻觉"的根本原因是？**
A. 训练数据量不够大
B. 模型基于上下文预测最可能的下文，而非查询事实
C. 用户提问不够清晰
D. 上下文窗口太小

<details>
<summary>答案与解析</summary>

**B**。幻觉源于生成机制本身：模型做的是概率性续写，不是事实检索。数据量、提问方式可以影响幻觉程度，但不是根本原因。
</details>

**3.（多选）以下哪些属于大模型的固有局限？**
A. 幻觉
B. 训练数据时间范围限制
C. 上下文长度限制
D. 无法处理英文

<details>
<summary>答案与解析</summary>

**ABC**。D 错误，主流大模型具备多语言能力。ABC 正是大纲列出的三大局限。
</details>

**4.（单选）关于大模型与搜索引擎的区别，下列说法正确的是？**
A. 两者本质相同，都是检索已有信息
B. 大模型生成内容，搜索引擎检索已有内容
C. 搜索引擎生成内容，大模型检索内容
D. 大模型的结果一定比搜索引擎准确

<details>
<summary>答案与解析</summary>

**B**。大模型是生成式，搜索引擎是检索式；准确性取决于具体场景，不能一概而论。
</details>

**5.（单选）某任务需要大模型计算"本月 128 个门店的销售总额"，最合适的增强方式是？**
A. 优化提示词让模型心算
B. 接入知识库
C. 调用计算/数据查询工具
D. 微调模型提升数学能力

<details>
<summary>答案与解析</summary>

**C**。确定性计算和实时业务数据应交给工具，模型负责理解需求和组织结果。让模型"心算"大量数据容易出错。
</details>

**6.【官方课后题】（单选）下面哪句话最准确地描述了大模型和 Agent 的关系？**
A. Agent 是大模型的一种参数规模
B. 大模型负责理解和生成，Agent 在此基础上接入工具、记忆和流程来完成任务
C. 大模型只能处理文字，Agent 只能处理图片
D. Agent 会取代大模型，独立完成理解、生成和工具调用

<details>
<summary>答案与解析</summary>

**B**。大模型提供理解、生成和推理能力；Agent 在此基础上接入工具、记忆和执行流程，让 AI 从"回答问题"进一步走向"完成任务"。A 错误，Agent 是应用方式不是参数规格；C 错误，大模型可处理文本、图像、音频等多种内容；D 错误，Agent 的核心仍然是大模型，不是脱离大模型单独工作的系统。
</details>