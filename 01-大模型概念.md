# 第 1 章 大模型概念（试题占比 18%）

> **大纲要求**：理解大模型是什么，能做什么，不能直接替人决定什么。理解大模型基于上下文生成内容的基本方式，以及由此带来的幻觉、训练数据时间范围和上下文限制。掌握能力边界判断：什么时候适合生成、总结、分析，什么时候需要知识库、工具或人工确认。

## 1.1 什么是大模型

- **大语言模型（LLM）**：在海量文本数据上训练的深度神经网络模型，通过学习语言的统计规律和世界知识，具备理解和生成自然语言的能力。
- **基本工作范式**：先用海量通用数据做**预训练**，再通过微调、对齐等方式适配具体任务（细节见第 4 章）。
- **生成式 AI**：大模型不仅能分类、检索，还能**生成**新内容——文本、代码、图像、音频等（多模态）。
- 大模型的本质是**从数据中学习模式**，而不是像数据库那样"记住"事实。

## 1.2 技术发展脉络（了解即可）

1. 早期统计语言模型 → 深度学习神经网络模型；
2. **Transformer 架构**出现，使模型能处理更长上下文、并行训练，模型规模大幅提升；
3. **"预训练 + 微调"范式**成熟：先在大规模语料上预训练通用模型，再针对下游任务适配；
4. 对话式大模型兴起（如 GPT 系列、通义千问等），大模型进入大规模应用阶段；
5. 当前趋势：**多模态**（文本/图像/语音统一处理）与 **Agent**（从回答问题到完成任务，见第 5 章）。

## 1.3 基本工作方式：基于上下文生成内容（高频考点）

- 大模型生成内容的核心机制是**逐词预测**：根据已有上下文，预测接下来最可能出现的词，循环往复直到完成。
- **上下文（Context）**：模型"看得见"的全部输入，包括系统指令、提供的资料和历史对话。**上下文窗口有长度上限**。
- 由此推出三个重要性质：
  1. 输出是**概率性**的——同一问题多次提问，答案可能不同；
  2. 模型并不"知道"事实，只是生成**看起来最合理**的续写 → 会产生幻觉；
  3. 模型的知识来自训练数据 → 有**时间边界**（见 1.4）。

## 1.4 三大固有局限（高频考点）

| 局限 | 含义 | 典型表现 | 应对方法 |
| --- | --- | --- | --- |
| **幻觉** | 自信地输出与事实不符的内容 | 编造不存在的文献、数据、法条 | RAG 提供事实依据、要求给出来源、人工核实 |
| **训练数据时间范围** | 模型只知道训练数据截止日期之前的知识 | 回答不了"今天的股价""最新的政策" | 工具调用联网检索、RAG 接入最新资料 |
| **上下文限制** | 输入超过窗口长度，早期信息会被截断或丢失 | 长对话"忘记"前面的要求 | 总结历史对话、长文档切分处理（呼应 RAG 切分） |

**幻觉的根源**：生成机制决定模型是在"预测最可能的下文"，不是在"查询事实库"；加上训练数据本身有噪声和错误。因此**幻觉只能减少、无法彻底消除**。

## 1.5 能力边界判断（核心考点）

考试重点不是背定义，而是**给一个场景，判断该用哪种方式**：

| 场景特征 | 正确做法 |
| --- | --- |
| 创意写作、润色改写、总结、翻译、头脑风暴 | 直接用大模型生成 |
| 规则明确的分类、格式转换、信息提取 | 直接用大模型（配合好的提示词） |
| 答案必须依据企业私有资料 / 专业文档 | 用 **RAG / 知识库**（第 3 章） |
| 需要实时数据、精确计算、执行业务动作 | 用**工具调用**（第 3 章） |
| 需要稳定的格式 / 风格 / 行为模式 | 考虑**微调**（第 4 章） |
| 医疗诊断、金融决策、合同签署、对外发布 | AI 辅助 + **人工确认**（第 6 章） |

**一句话口诀**：生成靠模型，事实靠 RAG，动作靠工具，稳定靠微调，责任靠人工。

## 1.6 典型应用场景

信息提取与摘要、内容生成与创作、知识问答与智能客服、资料分析与辅助决策、办公自动化、代码辅助等。

## 1.7 常见误区

- ❌ "大模型就是更聪明的搜索引擎" → 搜索引擎是检索已有网页，大模型是**生成**内容，两者机制不同；
- ❌ "模型回答得越流畅，说明它越'知道'事实" → 流畅来自语言模式学习，与事实正确性无必然关系；
- ❌ "换了更大的模型就不会有幻觉" → 幻觉是生成机制的固有属性，只能缓解不能根除；
- ❌ "AI 可以替人做最终决定" → 高风险场景的最终责任必须在人（见第 6 章）。

## 自测题

**1.（单选）用户询问"公司最新发布的差旅报销标准"，最合适的处理方式是？**
A. 直接让大模型回答
B. 让大模型联网搜索公开信息后回答
C. 通过 RAG 接入公司制度文档后回答
D. 对大模型进行微调后再回答

<details>
<summary>答案与解析</summary>

**C**。企业私有制度文档不在模型训练数据中，公开搜索也查不到内部制度；正确做法是用 RAG 接入公司文档。微调不适合用来"注入知识"（见第 4 章）。
</details>

**2.（单选）大模型产生"幻觉"的根本原因是？**
A. 训练数据量不够大
B. 模型基于上下文预测最可能的下文，而非查询事实
C. 用户提问不够清晰
D. 上下文窗口太小

<details>
<summary>答案与解析</summary>

**B**。幻觉源于生成机制本身：模型做的是概率性续写，不是事实检索。数据量、提问方式可以影响幻觉程度，但不是根本原因。
</details>

**3.（多选）以下哪些属于大模型的固有局限？**
A. 幻觉
B. 训练数据时间范围限制
C. 上下文长度限制
D. 无法处理英文

<details>
<summary>答案与解析</summary>

**ABC**。D 错误，主流大模型具备多语言能力。ABC 正是大纲列出的三大局限。
</details>

**4.（单选）关于大模型与搜索引擎的区别，下列说法正确的是？**
A. 两者本质相同，都是检索已有信息
B. 大模型生成内容，搜索引擎检索已有内容
C. 搜索引擎生成内容，大模型检索内容
D. 大模型的结果一定比搜索引擎准确

<details>
<summary>答案与解析</summary>

**B**。大模型是生成式，搜索引擎是检索式；准确性取决于具体场景，不能一概而论。
</details>

**5.（单选）某任务需要大模型计算"本月 128 个门店的销售总额"，最合适的增强方式是？**
A. 优化提示词让模型心算
B. 接入知识库
C. 调用计算/数据查询工具
D. 微调模型提升数学能力

<details>
<summary>答案与解析</summary>

**C**。确定性计算和实时业务数据应交给工具，模型负责理解需求和组织结果。让模型"心算"大量数据容易出错。
</details>
